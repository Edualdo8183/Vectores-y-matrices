Primer ejercicio 
import numpy as np

def es_identidad_con_numpy(matriz):
    return np.array_equal(matriz, np.eye(len(matriz)))

def es_identidad_sin_numpy(matriz):
    return all(matriz[i][j] == (1 if i == j else 0) for i in range(len(matriz)) for j in range(len(matriz))) 